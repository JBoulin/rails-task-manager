<h1>New Task</h1>

<%= form_with(model: @task, local: true) do |form| %>
<div class="container p-3 border bg-light rounded">
  <div class="row">
    <div class="mb-3">
      <%= form.label :title %>
      <%= form.text_field :title, class: 'form-control' %>
    </div>

    <div class="mb-3">
      <%= form.label :details %>
      <%= form.text_area :details, class: 'form-control' %>
    </div>

    <div class="mb-3">
      <%= form.label :completed, class: 'form-check-label' %>
      <%= form.check_box :completed, class: 'form-check-input' %>
    </div>

    <div class="mb-3">
      <%= form.submit 'Create Task', class: 'btn btn-primary' %>
    </div>
  <% end %>

  <%= link_to tasks_path, class: 'btn btn-secondary mt-3' do %>
    <i class="fa-solid fa-clock-rotate-left"></i> Back
  <% end %>
  </div>
</div>
